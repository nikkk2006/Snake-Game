#ifndef ENUMS
#define ENUMS

enum checkButtonPressed { START, SHOP, SETTINGS, EXIT};

enum class GameStates {
	MENU,
	START_DIFFICULT_WINDOW,
	START_GAME_OVER_WINDOW,
	START_THE_GAME,
	SHOP,
	SETTINGS,
	EXIT
};

#endif // !BUTTON_ENUMS
